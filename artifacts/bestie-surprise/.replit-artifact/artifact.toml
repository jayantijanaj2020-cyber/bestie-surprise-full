kind = "web"
previewPath = "/"
title = "Bestie Surprise"
version = "1.0.0"
id = "artifacts/bestie-surprise"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18687

[services.development]
run = "pnpm --filter @workspace/bestie-surprise run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bestie-surprise", "run", "build" ]
publicDir = "artifacts/bestie-surprise/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18687"
BASE_PATH = "/"
